import ll, {one, two, three, four} from './tests';
import removeKthNodeFromEnd from './removeKthNodeFromEnd';

const copy1 = Object.assign({}, one);
const copy2 = Object.assign({}, one);
const copy3 = Object.assign({}, one);
const copy4 = Object.assign({}, one);


describe('removeKthNodeFromEnd', () => {
  it('1', () => {
    expect(removeKthNodeFromEnd(ll, 0)).toEqual();
  });
  it('2', () => {

  })
})