
const balancedBrackets = (str) => {
  // initialize a stack
  const stack = [];
  // iterate string array and add bracket if open ['(', '[', '{'] 
  // or remove if closed [')',']','}']
  // if the last item of the stack does not match the open closing bracket, return false
};

module.exports = { balancedBrackets };